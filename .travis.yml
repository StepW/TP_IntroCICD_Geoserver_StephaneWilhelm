
language: java
os: linux
before_install:
  - rm ~/.m2/settings.xml
install:
  - pip install sphinx requests

jobs: 
  include: 
#    - stage: build
#      name: building-project
#      script: "sh './scripts/build.sh'"
    - stage: test
      name: test on jdk8
#      jdk: oraclejdk8
      script:
      - jdk_switcher use oraclejdk8
      - "sh './scripts/tests.sh'"
    - name: test on jdk11
#      jdk: oraclejdk11
      script: 
      - jdk_switcher use oraclejdk11
      - "sh './scripts/tests.sh'"
    - stage: deploy	
      name: deploying
      script: "sh './scripts/deploy.sh'"

